import * as packageJson from '../../package.json'

/**
 * `fast-alfred` default configuration
 */
export const DEFAULT_TAB_SIZE = 2

/**
 * Helpers
 */
export const PACKAGE_VERSION = packageJson.version
export const PACKAGE_NAME = packageJson.name
